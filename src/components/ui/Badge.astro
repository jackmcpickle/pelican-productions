---
import { cn } from '@/lib/cn';

interface Props {
    tone?: 'mint' | 'sun' | 'cream' | 'ink';
    class?: string;
}

const { tone = 'mint', class: className } = Astro.props;

const tones = {
    mint: 'bg-mint text-ink',
    sun: 'bg-mint text-ink',
    cream: 'bg-cream text-ink',
    ink: 'bg-ink text-white',
};
---

<span
    class={cn(
        'inline-flex items-center rounded-[800px] px-2.5 py-1 text-sm font-medium',
        tones[tone],
        className,
    )}
>
    <slot />
</span>
